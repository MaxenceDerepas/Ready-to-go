const Footer = () => {
    return (
        <footer>
            <p>
                Made with <span>React</span> at Le <span>Reacteur</span> by
                <span> Maxence</span>
            </p>
        </footer>
    );
};

export default Footer;
